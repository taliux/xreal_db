spring:
  datasource:
    url: jdbc:mysql://localhost:3306/xreal_tech_faq_test?useUnicode=true&characterEncoding=UTF-8&serverTimezone=UTC&createDatabaseIfNotExist=true
    username: xreal
    password: Xreal2025
    driver-class-name: com.mysql.cj.jdbc.Driver
  
  jpa:
    hibernate:
      ddl-auto: create-drop  # 测试时自动创建和删除表
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.MySQLDialect
    defer-datasource-initialization: false
  
  sql:
    init:
      mode: always
      schema-locations: classpath:schema.sql
      continue-on-error: true
  
  # 保持 Elasticsearch 配置（本地有 ES 服务）
  elasticsearch:
    uris: https://localhost:9200
    username: elastic
    password: s0Fov=RW4zmt4s_Pntd7
    connection-timeout: 10s
    socket-timeout: 30s
  
  data:
    elasticsearch:
      repositories:
        enabled: true
  
  ai:
    openai:
      api-key: sk-K6iNOdpFtpndLcX4P3WLjtkurQyfAyJ6HNUdisVgggzzWONl
      base-url: https://api.openai-hub.com
      embedding:
        options:
          model: text-embedding-3-small
    
    vectorstore:
      elasticsearch:
        index-name: test_xreal_tech_faq
        dimensions: 1536

server:
  port: 8080

logging:
  level:
    com.xreal: DEBUG
    org.springframework.data.elasticsearch: DEBUG